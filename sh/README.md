# Shell

The shell installation links (using a symbolic link) the `.${SHELL}rc`, where `${SHELL}` is either `bash` or `zsh`. The installation will move the old file (if it exists, to a `.old` version, to save it if yo want to rollback.)
