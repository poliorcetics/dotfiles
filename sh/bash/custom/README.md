# Bash customization

Files added in this repository are loaded before themes (except the default one, so its variables are usable here). You can most notably use a file here to pick a theme different from the default one.
